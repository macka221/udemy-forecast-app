import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  img_background: {
    flex: 1,
    backgroundColor: 'black',
  },
  image: {
    opacity: 0.75,
  },
});
